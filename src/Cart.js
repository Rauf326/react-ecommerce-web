import styled from "styled-components";
import React from "react";

const Cart = () => {
  return <Wrapper>
    <h2>hello cart</h2>
  </Wrapper>;
};

const Wrapper = styled.section`
  padding: 9rem 0;

`;

export default Cart;
